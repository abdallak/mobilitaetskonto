sap.ui.define(["sap/ui/core/mvc/Controller"], function (Controller) {
	"use strict";
	return Controller.extend("Mobilitaetskonto.Mobilitaetskonto.controller.Antrag", {
		/**
		 * Called when a controller is instantiated and its View controls (if available) are already created.
		 * Can be used to modify the View before it is displayed, to bind event handlers and do other one-time initialization.
		 * @memberOf Mobilitaetskonto.Mobilitaetskonto.view.Antrag
		 */
		onInit: function () {},
		/**
		 *@memberOf Mobilitaetskonto.Mobilitaetskonto.controller.Antrag
		 */
		Antrag_stellen: function (oEvent) {
			//This code was generated by the layout editor.
			
			 var beschreibung = this.getView().byId("beschreibung").getValue();
			var betrag = this.getView().byId("betrag").getValue();
			// var art = this.getView().byId("art").getValue();   falsch(kann nicht compilieren )
			this.getView().byId("beschreibung").setValue(betrag);
			switch ("one"){
				case "one":
					// this.getView().byId("beschreibung").setValue("Auszahlung");
					break;
				case "two":
					
					break;
				default:
				
				break; 
			}

			 //this.getView().byId("input0").setValue(text);
		}
	});
});